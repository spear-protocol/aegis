import BigNumber from 'bignumber.js';
export declare type StateUpdateChallengeConfig = {
    address: string;
    gasPrice: BigNumber;
    token?: string;
    gasLimit?: number;
};
export default function issueStateUpdateChallenge(config: StateUpdateChallengeConfig): Promise<string>;
