export declare enum NCEventType {
    TRANSFER_CONFIRMATION = "TRANSFER_CONFIRMATION"
}
export declare type SubscribeConfig = {
    address: string;
    token?: string;
    event: NCEventType;
    callback: (data: Object) => void;
};
export default function subscribe(config: SubscribeConfig): Function;
