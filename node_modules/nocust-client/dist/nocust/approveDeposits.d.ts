import BigNumber from 'bignumber.js';
export declare const APPROVE_AMOUNT: string;
export declare type ApproveDepositsConfig = {
    address: string;
    token: string;
    gasPrice: BigNumber | string;
};
export default function approveDeposits(config: ApproveDepositsConfig): Promise<string>;
