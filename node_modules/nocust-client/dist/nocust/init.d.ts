import { StorageEngine } from '../services/storage';
export declare type NOCUSTConfig = {
    contractAddress: string;
    rpcUrl: string;
    operatorUrl: string;
    storageEngine?: StorageEngine;
    privateKey?: string;
};
export default function init(config: NOCUSTConfig): Promise<void>;
