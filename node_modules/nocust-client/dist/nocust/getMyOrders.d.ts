import BigNumber from 'bignumber.js';
declare type OrderStatus = 'pending' | 'cancelled' | 'complete' | 'expired';
export declare type MyOrder = {
    txId: string;
    id: number;
    time: number;
    status: OrderStatus;
    baseTokenAddress: string;
    quoteTokenAddress: string;
    amount: BigNumber;
    price: BigNumber;
    eon: number;
    expiry?: number;
    type: string;
};
export default function getMyOrders(address: string, baseTokenAddress: string, quoteTokenAddress: string): Promise<MyOrder[]>;
export {};
