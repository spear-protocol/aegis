import BigNumber from 'bignumber.js';
import { Transaction } from '../wallet/transaction';
export declare type TransferConfig = {
    from: string;
    to: string;
    amount: BigNumber;
    token?: string;
    nonce?: BigNumber;
};
export default function transfer(config: TransferConfig): Promise<Transaction>;
