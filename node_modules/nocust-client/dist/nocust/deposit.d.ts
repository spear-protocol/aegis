import BigNumber from 'bignumber.js';
export declare type DepositConfig = {
    address: string;
    amount: BigNumber;
    gasPrice: string;
    token?: string;
    nonce?: number;
};
export default function deposit(config: DepositConfig): Promise<string>;
