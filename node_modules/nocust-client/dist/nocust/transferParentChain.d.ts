export declare type TransferParentChainConfig = {
    from: string;
    to: string;
    token?: string;
    amount: string;
    gasPrice: string;
};
export default function transferParentChain(config: TransferParentChainConfig): Promise<string>;
