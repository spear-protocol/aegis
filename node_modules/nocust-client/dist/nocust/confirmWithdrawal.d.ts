import BigNumber from 'bignumber.js';
export declare type ConfirmWithdrawalConfig = {
    address: string;
    gasPrice: BigNumber | string;
    token?: string;
};
export default function confirmWithdrawal(config: ConfirmWithdrawalConfig): Promise<string>;
