import BigNumber from 'bignumber.js';
export declare type WithdrawalConfig = {
    address: string;
    amount: BigNumber;
    gasPrice: string;
    token?: string;
};
export default function withdraw(config: WithdrawalConfig): Promise<string>;
