import BigNumber from 'bignumber.js';
export declare type RecoverFundsConfig = {
    address: string;
    gasPrice: BigNumber;
    token?: string;
};
export default function recoverFunds(config: RecoverFundsConfig): Promise<string>;
