import BigNumber from 'bignumber.js';
import { Wallet } from '../../wallet';
export declare function runChallengeChecks(wallet: Wallet): Promise<void>;
export default function issueStateUpdateChallenge(address: string, token: string, gas: number, gasPrice: BigNumber): Promise<any>;
