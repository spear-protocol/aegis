import BigNumber from 'bignumber.js';
import { Wallet } from '../../wallet';
declare type IssueDeliveryChallenge = {
    address: string;
    token: string;
    id: number;
    gasPrice: BigNumber;
};
export declare function deliveryChallengeChekcer(wallet: Wallet, id: number): Promise<boolean>;
export default function issueDeliveryChallenge(config: IssueDeliveryChallenge): Promise<void>;
export {};
