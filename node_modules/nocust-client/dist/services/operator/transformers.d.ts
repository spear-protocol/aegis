import { DepositPayload, TransactionPayload, ActiveStatePayload } from './payloads';
import { Transaction } from '../../wallet/transaction';
import { ActiveState } from 'wallet/activeState';
import { WalletId } from '../../wallet';
export declare const transformDepositFromServer: (data: DepositPayload) => import("../../wallet/parentChainTxs").ParentChainTransaction;
export declare const transformTransactionFromServer: (transactionData: TransactionPayload) => Transaction;
export declare const transformActiveStateFromServer: (wallet: WalletId, eon: number, acitveStateData: ActiveStatePayload) => ActiveState;
